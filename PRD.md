# Product Requirements Document (PRD)

## 1. ข้อมูลเอกสาร

| รายการ | รายละเอียด |
|---|---|
| ชื่อผลิตภัณฑ์ | ระบบบริหารจัดการข้อมูลการผลิตน้ำ |
| หน่วยงาน | งานผลิต การประปาส่วนภูมิภาคสาขาสุไหงโก-ลก |
| ขอบเขตเวอร์ชัน | UI MVP — แม่ข่ายสุไหงโก-ลก |
| แพลตฟอร์ม | Responsive Web Application |
| Tech stack | Next.js 16 App Router, TypeScript, Tailwind CSS, Supabase (เชื่อมต่อในระยะถัดไป) |
| ภาษา | ภาษาไทย |
| สถานะ | Draft สำหรับพัฒนา UI |

## 2. ภาพรวมผลิตภัณฑ์

ระบบศูนย์กลางสำหรับบันทึก ติดตาม ตรวจสอบ วิเคราะห์ และแสดงผลข้อมูลการผลิตน้ำของแม่ข่ายสุไหงโก-ลก โดยแทนที่การรวบรวมข้อมูลที่กระจัดกระจายด้วย workflow ที่กำหนดรอบเวลา ตรวจข้อมูลซ้ำ คำนวณผลต่างของมาตร และให้ผู้ใช้งานตรวจทานก่อนยืนยัน

MVP นี้เน้นการออกแบบประสบการณ์ใช้งานและ frontend ด้วย mock data ยังไม่ทำ authentication, database หรือ Supabase integration จริง แต่โครงสร้างต้องพร้อมต่อยอดโดยแยกชั้น UI, domain logic และ data access ออกจากกัน

## 3. เป้าหมาย

- ทำให้การบันทึกค่ามาตรตามรอบเวลาครบถ้วนและเป็นมาตรฐาน
- ลดความผิดพลาดจากข้อมูลซ้ำ ฟิลด์ว่าง และผลต่างค่ามาตรติดลบ
- ทำให้ผู้บันทึกตรวจสอบค่าก่อนหน้า ผลต่าง และข้อมูลทั้งหมดก่อนยืนยัน
- ทำให้ผู้บริหารและผู้ตรวจสอบเห็นภาพรวม แนวโน้ม และความครบถ้วนของข้อมูล
- เตรียมฐาน frontend สำหรับเชื่อม Supabase, API และ Sensor/IoT ในอนาคต

## 4. สิ่งที่ไม่อยู่ในขอบเขต MVP

- การเชื่อมต่อ Supabase และฐานข้อมูลจริง
- Authentication และ authorization ฝั่ง server
- การรับข้อมูลอัตโนมัติจาก Sensor/IoT
- การนำเข้าหรือส่งออกไฟล์จริง
- Notification ภายนอก เช่น LINE, SMS หรืออีเมล
- การรองรับสถานีอื่นนอกเหนือจากแม่ข่ายสุไหงโก-ลก
- workflow อนุมัติหลังบันทึกโดย Supervisor จนกว่าจะได้รับข้อสรุป

## 5. กลุ่มผู้ใช้และสิทธิ์

| ความสามารถ | Admin | Manager | Supervisor | Operator | Viewer |
|---|:---:|:---:|:---:|:---:|:---:|
| Dashboard ภาพรวม | ✓ | ✓ | ✓ | เฉพาะสถานี | ✓ |
| บันทึกมาตรน้ำดิบ | ✓ | — | ✓ | ✓ | — |
| บันทึกมาตรหลักทางจ่าย | ✓ | — | ✓ | ✓ | — |
| ดูประวัติข้อมูล | ✓ | ✓ | ✓ | เฉพาะสถานี | ✓ |
| แก้ไขข้อมูล | ✓ | — | เฉพาะขอบเขต | ตามนโยบายที่กำหนด | — |
| ดูรายงาน/KPI | ✓ | ✓ | ✓ | เฉพาะสถานี | ✓ |
| จัดการผู้ใช้และ Role | ✓ | — | — | — | — |
| ตั้งค่าระบบ | ✓ | — | — | — | — |

หมายเหตุ: ใน UI MVP ใช้ role switcher สำหรับจำลองสิทธิ์เท่านั้น ไม่ใช่ระบบรักษาความปลอดภัยจริง

## 6. Information Architecture

Sidebar หลัก:

- ภาพรวม
- แม่ข่ายสุไหงโก-ลก (Expand/Collapse)
  - บันทึกข้อมูล (เปิดหน้าเลือกแบบฟอร์ม)
    - แบบฟอร์มมาตรน้ำดิบ
    - แบบฟอร์มมาตรหลักทางจ่าย
  - Dashboard / รายงาน
- จัดการผู้ใช้ (Admin)
- ตั้งค่าระบบ (Admin)

บนหน้าจอขนาดเล็ก Sidebar เปลี่ยนเป็น Drawer และต้องปิดได้ด้วยปุ่ม, การเลือกเมนู และปุ่ม Escape

## 7. Functional Requirements

### FR-00 การเข้าสู่ระบบและขอใช้งานครั้งแรก

- หน้าแรกสำหรับผู้ที่ยังไม่ยืนยันตัวตนต้องเป็นหน้าเข้าสู่ระบบ ไม่แสดง Dashboard หรือข้อมูลการผลิต
- ผู้ใช้เดิมเข้าสู่ระบบด้วยรหัสพนักงานหรืออีเมลและรหัสผ่าน
- ผู้ใช้ครั้งแรกส่งคำขอด้วยรหัสพนักงาน ชื่อ–นามสกุล ตำแหน่ง เบอร์โทรศัพท์ และอีเมล
- รหัสพนักงานและอีเมลต้องไม่ซ้ำเมื่อเชื่อม backend และต้องตรวจรูปแบบเบอร์โทรกับอีเมล
- ผู้สมัครต้องยืนยันความถูกต้องและยอมรับการใช้ข้อมูลก่อนส่งคำขอ
- หลังส่งคำขอแสดงเลขที่คำขอและสถานะรอตรวจสอบ
- Admin เป็นผู้อนุมัติหรือไม่อนุมัติ กำหนด Role สถานี และสถานะบัญชี
- ผู้สมัครไม่สามารถเลือก Role หรือเปิดใช้งานบัญชีเองได้
- UI MVP จำลอง flow ด้วย state ภายในหน้า; Authentication และ email verification จริงอยู่ในระยะ Supabase

### FR-01 แบบฟอร์มบันทึกมาตรน้ำดิบ

ฟิลด์บังคับ:

- วันที่
- ช่วงเวลา: 00.00 น., 06.00 น., 14.00 น., 22.00 น.
- มาตรน้ำดิบ 1
- มาตรน้ำดิบ 2

พฤติกรรม:

1. ค่าเริ่มต้นของวันที่เป็นวันที่ปัจจุบันและแก้ไขได้
2. เลือกช่วงเวลาได้จากค่าที่กำหนดเท่านั้น
3. ค่ามาตรรับเฉพาะตัวเลขศูนย์หรือค่าบวก และรองรับทศนิยม
4. ระบบแสดงค่าครั้งก่อนของมาตรแต่ละตัว
5. ระบบคำนวณ `ผลต่าง = ค่าปัจจุบัน - ค่าครั้งก่อน` แบบทันที
6. หากผลต่างติดลบ ต้องแสดงข้อผิดพลาดระดับฟิลด์และไม่อนุญาตให้ไปขั้นยืนยัน
7. หากคู่วันที่และช่วงเวลาซ้ำกับรายการเดิม ต้องแจ้งเตือนและไม่อนุญาตให้บันทึก
8. เมื่อข้อมูลผ่าน validation ปุ่ม “ตรวจสอบข้อมูล” จะพร้อมใช้งาน

### FR-02 แบบฟอร์มบันทึกมาตรหลักทางจ่าย

ฟิลด์บังคับ:

- วันที่
- ช่วงเวลา: 00.00–23.00 น. รวม 24 ค่า
- มาตรหลักโซนสูง
- มาตรหลักโซนต่ำ
- มาตรหลักแว้ง

ใช้กฎการตรวจสอบ การคำนวณผลต่าง และการตรวจรายการซ้ำเช่นเดียวกับ FR-01

### FR-03 ขั้นตอนตรวจสอบและยืนยัน

1. ผู้ใช้กรอกข้อมูลในหน้าแบบฟอร์ม
2. ระบบตรวจสอบข้อมูลทุกฟิลด์และคำนวณผลต่าง
3. ผู้ใช้กด “ตรวจสอบข้อมูล” เพื่อเปิดหน้าต่างสรุป
4. หน้าต่างสรุปแสดงวันที่ เวลา ค่าครั้งก่อน ค่าปัจจุบัน และผลต่างของทุกมาตร
5. ผู้ใช้เลือก “กลับไปแก้ไข” หรือ “ยืนยันและบันทึก”
6. เมื่อยืนยันสำเร็จ UI แสดง success feedback และเพิ่มรายการลงใน mock history
7. ระบบป้องกันการกดยืนยันซ้ำระหว่างสถานะกำลังบันทึก

### FR-04 Dashboard

Dashboard MVP ต้องประกอบด้วย:

- สถานะความครบถ้วนของรอบบันทึกวันนี้
- ค่าผลต่างล่าสุดของมาตรน้ำดิบ 1 และ 2
- ค่าผลต่างล่าสุดของโซนสูง โซนต่ำ และแว้ง
- แนวโน้มปริมาณน้ำตามเวลา
- รายการบันทึกล่าสุด พร้อมผู้บันทึกและสถานะ
- ตัวกรองวันที่และประเภทมาตร

ข้อมูลทั้งหมดในระยะนี้ต้องมีป้ายกำกับว่า “ข้อมูลตัวอย่าง”

### FR-05 รายงานและประวัติ

- รายงานปริมาณน้ำดิบ: รายกะ รายวัน รายเดือน และรายปีงบประมาณ
- รายกะแบ่งเป็น 3 กะ: กะที่ 1 เวลา 06.00–14.00 น., กะที่ 2 เวลา 14.00–22.00 น. และกะที่ 3 เวลา 22.00–06.00 น. ของวันถัดไป
- รายงานปริมาณน้ำผลิตจ่าย: รายชั่วโมง รายวัน รายเดือน และรายปีงบประมาณ
- รายงานปริมาณน้ำสูญเสียในระบบผลิต: รายวัน รายเดือน และรายปีงบประมาณ
- สูตรน้ำสูญเสีย: `(ปริมาณน้ำดิบ - ปริมาณน้ำผลิตจ่าย) / ปริมาณน้ำดิบ × 100`
- เป้าหมายน้ำสูญเสียในระบบผลิตต้องไม่เกิน 5% และต้องแสดงสถานะผ่าน/เกินเป้าหมายโดยไม่ใช้สีเพียงอย่างเดียว
- แต่ละรายงานต้องมีตัวเลือกช่วงเวลา KPI กราฟ และตารางรายละเอียด
- เมื่อเปลี่ยนช่วงข้อมูล KPI กราฟ และตารางรายละเอียดต้องเปลี่ยนข้อมูลและป้ายกำกับให้สอดคล้องกันทั้งหมด
- ประวัติรายการบันทึกล่าสุดยังเข้าถึงได้จากหน้ารายงาน
- ประเภทรายงานและช่วงเวลาต้องประกาศผ่าน Report Registry เพื่อรองรับการเพิ่มรายงานโดยไม่ผูก logic กับ navigation
- ปุ่มส่งออกให้เพิ่มในระยะเชื่อม backend

### FR-06 การจำลอง Role

- มี role switcher ใน developer/demo UI
- เมนูและ action ต้องเปลี่ยนตาม access matrix
- การเข้าถึง URL ที่ไม่มีสิทธิ์ต้องแสดงหน้า Access denied หรือ redirect ไปหน้าที่เข้าถึงได้
- ต้องมีข้อความชัดเจนว่าการจำลองนี้ไม่ใช่ security enforcement จริง

### FR-07 การแก้ไขและยกเลิกข้อมูลหลังยืนยัน

- Operator แก้ไขรายการของตนเองได้ก่อนปิดกะ และส่งคำขอยกเลิกได้โดยต้องระบุเหตุผล
- Supervisor แก้ไขรายการในสถานีที่รับผิดชอบ และยกเลิกหรือพิจารณาคำขอยกเลิกได้
- Admin แก้ไข ยกเลิก และกู้คืนรายการได้ทั้งหมด
- Manager และ Viewer ดูข้อมูลและ Audit Log ได้อย่างเดียว
- การแก้ไขต้องเก็บค่าเดิม ค่าใหม่ ผู้แก้ไข วันเวลา และเหตุผล
- การยกเลิกใช้ Soft Delete ไม่ลบ record ออกจากฐานข้อมูล
- สถานะประกอบด้วย `active`, `cancellation_requested` และ `cancelled`
- รายการที่ยกเลิกแล้วไม่นำไปคำนวณรายงาน แต่ยังค้นหาและเปิดดูประวัติได้
- การยกเลิกรายการกลางลำดับต้องทำให้ระบบคำนวณผลต่างของรายการถัดไปใหม่เมื่อเชื่อม backend
- การกู้คืนทำได้เฉพาะ Admin และต้องถูกบันทึกใน Audit Log
- รายการบันทึกและเครื่องมือแก้ไขต้องอยู่ภายในรายงานต้นทางของตนเอง: มาตรน้ำดิบอยู่ในรายงานปริมาณน้ำดิบ และมาตรหลักทางจ่ายอยู่ในรายงานปริมาณน้ำผลิตจ่าย
- รายงานน้ำสูญเสียในระบบผลิตเป็นข้อมูลสรุปจากข้อมูลต้นทางเท่านั้น จึงไม่มีการแก้ไขหรือยกเลิกรายการจากหน้ารายงานน้ำสูญเสีย

## 8. Validation และ Business Rules

| รหัส | กฎ |
|---|---|
| BR-01 | ทุกฟิลด์ในแบบฟอร์มเป็น required |
| BR-02 | วันที่ต้องเป็นวันที่ที่ระบบยอมรับและต้องไม่อยู่อนาคต เว้นแต่กำหนดนโยบายใหม่ |
| BR-03 | ช่วงเวลาต้องอยู่ในรายการของแบบฟอร์มนั้น |
| BR-04 | วันที่ + ช่วงเวลา + ประเภทแบบฟอร์ม + สถานี ต้องไม่ซ้ำ |
| BR-05 | ค่ามาตรต้องเป็นจำนวนเต็ม ไม่มีจุดทศนิยม และมีค่าตั้งแต่ 0 ขึ้นไป |
| BR-06 | ค่าปัจจุบันต้องไม่น้อยกว่าค่าครั้งก่อนของมาตรเดียวกัน |
| BR-07 | ผลต่างคำนวณจากค่าปัจจุบันลบค่าครั้งก่อนและต้องไม่ติดลบ |
| BR-08 | ห้ามบันทึกจนกว่าผู้ใช้จะยืนยันข้อมูลใน confirmation dialog |
| BR-09 | การแก้ไขข้อมูลหลังบันทึกต้องเก็บ audit trail เมื่อเชื่อม backend ในอนาคต |

ข้อความผิดพลาดต้องระบุสาเหตุและแนวทางแก้ไข เช่น “ค่าปัจจุบันน้อยกว่าค่าครั้งก่อน 250.00 ลบ.ม. กรุณาตรวจสอบเลขมาตร”

## 9. UX Requirements

- Desktop-first สำหรับงานประจำสถานี และ responsive ถึง mobile ขนาด 360px
- โครงหน้าจอเน้นความรวดเร็วในการกรอกและการตรวจทาน
- แสดงค่าครั้งก่อนและผลต่างใกล้ช่องกรอก ไม่ซ่อนใน tooltip
- ห้ามใช้สีเป็นตัวสื่อ error/success เพียงอย่างเดียว ต้องมี icon และข้อความ
- รองรับ keyboard navigation และ focus visibility
- Modal ต้อง trap focus, ปิดด้วย Escape และคืน focus ไปยังปุ่มเดิม
- Loading, empty, error, duplicate และ success states ต้องมีหน้าตาเฉพาะ
- ตัวเลขใช้รูปแบบสม่ำเสมอและจัดแนวเพื่อเปรียบเทียบได้ง่าย

## 10. Visual Direction

- โทนหลักฟ้า–ขาว สดใส สะอาด และน่าเชื่อถือ
- ให้พื้นที่ข้อมูลและแบบฟอร์มเด่นกว่างานตกแต่ง
- ใช้สีฟ้าเข้มสำหรับโครงสร้าง/ตัวอักษรหลัก ฟ้าสว่างสำหรับสถานะ active และพื้นขาวหรือฟ้าอ่อนสำหรับพื้นที่ทำงาน
- สีเขียวใช้เฉพาะ success และสีแดงใช้เฉพาะ error/critical
- หลีกเลี่ยง gradient ที่รบกวนการอ่าน glassmorphism และ card จำนวนมากเกินจำเป็น

## 11. Non-functional Requirements

- TypeScript strict mode และไม่มี `any` ที่ไม่จำเป็น
- ใช้ semantic HTML และผ่านแนวทาง WCAG 2.1 AA ในส่วนหลัก
- หน้าแรกควรแสดง usable content ได้รวดเร็วบนเครือข่ายภายในทั่วไป
- Component และ domain types ต้องนำกลับมาใช้ซ้ำได้
- business rules ต้องแยกเป็น pure functions เพื่อย้ายไปใช้กับ backend validation ได้
- UI ต้องไม่ฝัง Supabase SDK ไว้ใน component โดยตรง
- รองรับ locale ไทยและ timezone Asia/Bangkok

## 12. Proposed Frontend Architecture

```text
app/
  dashboard/
  records/raw-water/
  records/distribution/
  reports/
  users/
  settings/
components/
  layout/
  forms/
  dashboard/
  ui/
features/
  meter-readings/
    types.ts
    validation.ts
    calculations.ts
    mock-data.ts
lib/
  permissions.ts
  formatters.ts
```

เมื่อเชื่อม Supabase ให้เพิ่ม repository/data adapter โดยรักษา interface เดิมของ feature layer

## 13. Domain Model เบื้องต้น

### MeterReading

- id
- stationId
- meterType: `raw_water` หรือ `distribution`
- readingDate
- readingTime
- values: object ตามชนิดมาตร
- previousValues
- differences
- recordedBy
- recordedAt
- updatedAt
- status

Unique constraint ที่ต้องเตรียมในอนาคต: `(station_id, meter_type, reading_date, reading_time)`

## 14. Acceptance Criteria

### AC-01 มาตรน้ำดิบ

- ผู้ใช้กรอกข้อมูลครบและดูผลต่างของมาตรทั้งสองได้ก่อนยืนยัน
- เลือกได้เฉพาะ 4 ช่วงเวลาที่กำหนด
- ระบบไม่ยอมรับรายการวันที่และเวลาซ้ำ
- ระบบไม่ยอมรับค่าที่ทำให้ผลต่างติดลบ
- ผู้ใช้กลับไปแก้ไขจากหน้าสรุปได้

### AC-02 มาตรหลักทางจ่าย

- ผู้ใช้เลือกเวลาได้ครบ 24 ชั่วโมง
- ระบบคำนวณผลต่างของทั้งสามมาตร
- validation และ confirmation ทำงานเช่นเดียวกับแบบฟอร์มน้ำดิบ

### AC-03 Responsive และสิทธิ์

- Sidebar expand/collapse ได้บน desktop และเป็น drawer บน mobile
- role switcher ทำให้เมนูและ action เปลี่ยนตาม access matrix
- ทุก interactive control ใช้งานผ่าน keyboard ได้

### AC-04 คุณภาพโค้ด

- `npm run build` สำเร็จ
- lint/type check สำเร็จ
- ไม่มีการเรียก Supabase หรือ network API จริง
- mock data ถูกแยกจาก presentation components

## 15. KPI ของผลิตภัณฑ์ในอนาคต

- อัตราการบันทึกข้อมูลครบตามรอบเวลา
- จำนวนรายการซ้ำหรือรายการที่ validation ปฏิเสธ
- ระยะเวลาเฉลี่ยในการบันทึกหนึ่งรอบ
- จำนวนการแก้ไขข้อมูลหลังบันทึก
- ความพร้อมใช้งานของข้อมูลสำหรับรายงานประจำวัน

## 16. Roadmap

### Phase 1 — UI MVP

App shell, Sidebar, Dashboard, แบบฟอร์ม 2 ประเภท, validation, confirmation, mock history, role simulation และ responsive UI

### Phase 2 — Supabase

Authentication, RBAC/RLS, database schema, CRUD, audit log, server-side uniqueness validation และรายงานจากข้อมูลจริง

### Phase 3 — Integration

API, Sensor/IoT ingestion, alerting, data-quality monitoring และรองรับหลายสถานี

## 17. ความเสี่ยงและประเด็นรอตัดสินใจ

- ค่ามาตรอาจ rollover หรือถูกเปลี่ยนเครื่อง ทำให้ผลต่างติดลบโดยถูกต้องตามเหตุการณ์จริง ต้องกำหนด exception workflow ก่อนเชื่อมข้อมูลจริง
- ต้องกำหนดจำนวนตำแหน่งทศนิยมและหน่วยของค่ามาตรอย่างเป็นทางการ
- ต้องกำหนดสิทธิ์ Operator ในการแก้ไขข้อมูลหลังยืนยันและช่วงเวลาที่อนุญาต
- ต้องตัดสินใจว่า Supervisor ต้องตรวจรับ/อนุมัติทุก record หรือเฉพาะ record ผิดปกติ
- ต้องยืนยันนโยบายการเลือกวันที่ย้อนหลังและ cutoff ของแต่ละกะ
